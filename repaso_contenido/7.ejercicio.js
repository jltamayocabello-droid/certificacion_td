let valores = [5, 6, 7, 6, 9];

let indice = 0;

while(indice < valores.length){
    console.log(valores[indice]);
    indice++;
};

console.log("Terminado.");

// Do While (Cuando el código debe ejecutarse al menos una vez)
do {
    console.log(valores[indice]);
    indice++;

} while(true)